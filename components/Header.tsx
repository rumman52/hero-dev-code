'use client';
import { Menu, X } from 'lucide-react';import { useState } from 'react';import { contact, navItems } from '@/lib/portfolio-data';import { useMotionSetting } from './MotionProvider';
export function Header(){const [open,setOpen]=useState(false);const {enabled,toggle}=useMotionSetting();const nav=<>{navItems.map(n=><a key={n} onClick={()=>setOpen(false)} className="px-3 py-2 text-sm muted hover:text-paper" href={`#${n.toLowerCase()}`}>{n}</a>)}</>;return <header className="fixed inset-x-0 top-0 z-40 border-b border-white/10 bg-ink/80 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><a href="#top" className="font-display text-lg font-bold">Md Rumman Ali <span className="text-tech">/ Solve.</span></a><nav className="desktop-nav flex items-center gap-1" aria-label="Primary">{nav}</nav><div className="hidden items-center gap-2 md:flex"><button className="focus-ring min-h-11 border border-white/15 px-3 text-sm" onClick={toggle}>Motion {enabled?'On':'Off'}</button><a className="btn" href={`mailto:${contact.email}`}>Let’s Solve Something</a></div><button className="mobile-only hidden min-h-11 min-w-11" aria-label="Toggle menu" aria-expanded={open} onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-white/10 bg-surface p-4 md:hidden"><nav className="grid" aria-label="Mobile">{nav}</nav><button className="my-3 min-h-11 border border-white/15 px-3 text-sm" onClick={toggle}>Motion {enabled?'On':'Off'}</button><a className="btn w-full" href={`mailto:${contact.email}`}>Let’s Solve Something</a></div>}</header>}
